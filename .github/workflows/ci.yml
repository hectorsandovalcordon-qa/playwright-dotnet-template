name: .NET Tests with Allure

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0.x'

      - name: List project root files and folders
        run: ls -l

      - name: List tests/UnitTests folder recursively
        run: ls -lR tests/UnitTests

      - name: List output bin folder recursively
        run: ls -lR tests/UnitTests/bin/Debug/net9.0

      - name: Build solution
        run: dotnet build ./tests/UnitTests/UnitTests.csproj --configuration Debug

      - name: Copy allureConfig.json to output folder
        run: cp tests/UnitTests/allureConfig.json tests/UnitTests/bin/Debug/net9.0/allureConfig.json

      - name: Run tests with Allure logger
        env:
          ALLURE_CONFIG: tests/UnitTests/bin/Debug/net9.0/allureConfig.json
        run: |
          mkdir -p tests/UnitTests/bin/Debug/net9.0/allure-results
          dotnet test ./tests/UnitTests/UnitTests.csproj \
            --no-build \
            --configuration Debug \
            --logger "trx;LogFileName=test-results.trx" \
            --logger "Allure"

      - name: List allure-results folder contents
        run: ls -l tests/UnitTests/bin/Debug/net9.0/allure-results

      # Opcional: sube resultados como artifacts para revisar fuera del job
      - name: Upload Allure Results Artifact
        uses: actions/upload-artifact@v3
        with:
          name: allure-results
          path: tests/UnitTests/bin/Debug/net9.0/allure-results
